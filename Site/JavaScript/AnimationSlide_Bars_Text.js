window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element1");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in1");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element2");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in2");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element3");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in3");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element4");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in4");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element5");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in5");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element6");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in6");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element7");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in7");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element8");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in8");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element9");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in9");
  }
});

window.addEventListener("scroll", function () {
  var element = document.getElementById("my-element10");
  var elementPosition = element.getBoundingClientRect().top;
  var screenPosition = window.innerHeight / 0.4;

  if (elementPosition < screenPosition) {
    element.classList.remove("hidden");
    element.classList.add("slide-in10");
  }
});
